# v0.8.1 achievement-log-layout hotfix

v0.8.1 是 v0.8 成就系统之后的小修版本，重点修复运行稳定性和界面拥挤问题。

## 主要变化

- 修复普通行动解锁成就后没有立刻保存的问题。
- 一次触发多个成就时可以一起显示。
- 连续触发成就提示时，会取消上一次的清除计时器，避免新提示被提前清空。
- 成就配置读取增加防御，缺少字段时不会直接崩溃。
- 行动记录只显示最后 10 条，避免日志区域往下溢出。
- 行动按钮改成 3 列自动排列，新增按钮后会自动换行。

## 快照文件

- `history/v0.8.1 achievement-log-layout hotfix.py`
